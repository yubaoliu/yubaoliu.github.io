* 2015-SLAM++: Simultaneous Localisation and Mapping at the Level of
Objecs
  :PROPERTIES:
  :CUSTOM_ID: slam-simultaneous-localisation-and-mapping-at-the-level-of-objecs
  :END:

** SLAM++: Simultaneous Localisation and Mapping at the Level of Objecs
   :PROPERTIES:
   :CUSTOM_ID: slam-simultaneous-localisation-and-mapping-at-the-level-of-objecs-1
   :END:

*Authors*: Renato F. Salas-Moreno, Richard A. Newcombe, Hauke Strasdat,
Paul H.J. Kelly, *Andrew J. Davison*[1]

[1] Professor, Department of Computing, Imperial College London, vision
and AI technology for next generation home robotics

*Cite*:The IEEE Conference on Computer Vision and Pattern Recognition
(CVPR), 2013, pp. 1352-1359

--------------

** Author Introduction: *Andrew J. Davison*
   :PROPERTIES:
   :CUSTOM_ID: author-introduction-andrew-j.-davison
   :END:

- Currently his main research interests are in improving the performance
  in terms of *dynamics, scale, detail level, efficiency and semantic
  understanding of real-time 3D vision*

- He believes that SLAM is evolving into something even more important
  that I am calling "*Spatial
  AI*"(Refer:[[https://arxiv.org/pdf/1803.11288.pdf][*FutureMapping: The
  Computational Structure of Spatial AI Systems*]] )

--------------

** Overview
[[https://www.youtube.com/watch?v=tmrAh1CqCRo&t=1s][Demo-Youtube]]
   :PROPERTIES:
   :CUSTOM_ID: overview-demo-youtube
   :END:

[[/Assets/images/papers/SLAM++/SLAMPlusPlus_YoutubeDemo.png]]

** SLAM++ - A Pure Object-Level SLAM System
   :PROPERTIES:
   :CUSTOM_ID: slam---a-pure-object-level-slam-system
   :END:

In SLAM++, a cluttered 3D scene is efficiently *tracked and mapped in
real-time* directly at the *object level*.

A new *object oriented* 3D SLAM paradigm, which takes full advantage in
the loop of prior knowledge that many scenes consist of repeated,
domain-specific objects and structures.

** Abstract
   :PROPERTIES:
   :CUSTOM_ID: abstract
   :END:

Demonstrate real-time incremental SLAM: - in large, cluttered
environments, *scenes consit of repeated, domain-specific objects and
structures* - with relocalisation and loop closure - with the detection
of moved objects - with *the generation of an object level scene
description* - with the potential to enable *interaction*

--------------

** Introduction - Current SLAM
   :PROPERTIES:
   :CUSTOM_ID: introduction---current-slam
   :END:

Most current real-time SLAM systems operate at the level of - *low-level
primitives* (points, lines, patches or non-parametric surface
representations such as depth maps), which must be - *robustly
matched*, - *geometrically transformed* - and *optimised over* in order
that - *maps* represent *the intricacies of the real world*.

--------------

** Introduction - Object SLAM
   :PROPERTIES:
   :CUSTOM_ID: introduction---object-slam
   :END:

- Much interest is now turning to *semantic labelling* of this geometry
  in terms of the objects and regions that are known to exist in the
  scene

However, this maybe reveals *a huge amount of wasted computational
effort* - The potential for a much better way of *taking account of
domain knowledge in the loop of SLAM operation itself* - Object SLAM has
many characteristics of a return to feature-based SLAM methods

** SLAM++ - A Pure Object-Level SLAM System
   :PROPERTIES:
   :CUSTOM_ID: slam---a-pure-object-level-slam-system-1
   :END:

L: A live view at the *current camera pose* and the *synthetic rendered
objects*

R: Contrast *a raw depth camera normal map* with *the corresponding high
quality prediction* from object graph

--------------

** Mapping Directly at the Object Level
   :PROPERTIES:
   :CUSTOM_ID: mapping-directly-at-the-object-level
   :END:

** Advantages:
   :PROPERTIES:
   :CUSTOM_ID: advantages
   :END:

1. Instant *semantic understanding* enables *interaction* (Robotics, AR)
2. Dense representation and full predictive power of *KinectFusion* but
   *very memory-efficient*
3. Some aspects of a return to feature-based paradigm --- full joint
   *optimisation of a graph* in real-time is feasible

--------------

** System Overview
   :PROPERTIES:
   :CUSTOM_ID: system-overview
   :END:

#+BEGIN_HTML
  <center>
#+END_HTML

#+BEGIN_HTML
  </center>
#+END_HTML

*Real-Time Loop*:  - Currently uses depth data only (no RGB). - Most
components implemented primarily on GPU (not the pose graph optimisation
which uses g2o).

--------------

** Methods - Overview
   :PROPERTIES:
   :CUSTOM_ID: methods---overview
   :END:

$D_l$: a live depth map; $N_l$ : normal map First compute a surface
measurement in the form of a vertex and $N_l$ providing to the
sequentially computed camera tracking and object detection pipelines

[[/Assets/images/papers/SLAM++/Outline_of_the_SLAM++_pipeline.png]]

--------------

** Method - Overview - Step 1
   :PROPERTIES:
   :CUSTOM_ID: method---overview---step-1
   :END:

*Track the live camera pose $T_{wl}$* with an iterative closest point
(ICP) approach using the dense multi-object scene prediction captured in
the current SLAM graph $G$
[[/Assets/images/papers/SLAM++/Outline_of_the_SLAM++_pipeline.png]]

--------------

** Method - Overview - Step 2
   :PROPERTIES:
   :CUSTOM_ID: method---overview---step-2
   :END:

Attempt to *detect objects*, previously stored in a database, that are
present in the live frame, *generating detection candidates* with *an
estimated pose* in the scene. Candidates are first refined or rejected
using a second ICP estimation initialised with the detected pose
[[/Assets/images/papers/SLAM++/Outline_of_the_SLAM++_pipeline.png]]

--------------

** Method - Overview - Step 3
   :PROPERTIES:
   :CUSTOM_ID: method---overview---step-3
   :END:

Add successfully *detected objects $g$* into the SLAM graph in the form
of a *object-pose* vertex connected to the live estimated *camera-pose*
vertex via a measurement edge
[[/Assets/images/papers/SLAM++/Outline_of_the_SLAM++_pipeline.png]]

--------------

** Method - Overview - Step 4
   :PROPERTIES:
   :CUSTOM_ID: method---overview---step-4
   :END:

Rendering objects from the SLAM graph produce a *predicted depth $D_r$*
and *normal map $N_r$* into the live estimated frame, enabling us to
actively *search only those pixels not described by current objects in
the graph*. Run an individual ICP between each object and the live image
resulting in the addition of a new camera-object constraint into the
SLAM graph.
[[/Assets/images/papers/SLAM++/Outline_of_the_SLAM++_pipeline.png]]

--------------

** Object Database
   :PROPERTIES:
   :CUSTOM_ID: object-database
   :END:

*Currently a small number of precisely known objects*:

Carefully scanned using *KinectFusion* and manually segmented

--------------

** Real-Time Object Detection
   :PROPERTIES:
   :CUSTOM_ID: real-time-object-detection
   :END:

#+CAPTION: center 60%
[[/Assets/images/papers/SLAM++/SLAM++-RealTimeObjectDetection.png]]

From 'Model Globally, Match Locally', Drost et al., CVPR 2010 - Each
object is described in terms of all point-pair features (PPF) across
their whole 3D surface - Point pair features are grouped and stored in a
hash table

--------------

** Real-Time Object Detection - PPF
   :PROPERTIES:
   :CUSTOM_ID: real-time-object-detection---ppf
   :END:

#+BEGIN_HTML
  <center>
#+END_HTML

#+BEGIN_HTML
  </center>
#+END_HTML

$F$: PPF of two oriented points $F_1$: the distance of the points $F_2$:
the angle between the normals and $F_1$ $F_4$: the angle between the two
normals

--------------

** Real-Time Object Detection
   :PROPERTIES:
   :CUSTOM_ID: real-time-object-detection-1
   :END:

#+BEGIN_HTML
  <center>
#+END_HTML

#+BEGIN_HTML
  </center>
#+END_HTML

The global model description Left: Point pairs on the model surface with
*similar feature vector $F$* Right: Those point pairs are stored in the
same slot in the hash table

--------------

** ICP Algorithm Overview
   :PROPERTIES:
   :CUSTOM_ID: icp-algorithm-overview
   :END:

#+CAPTION: 50% center
[[/Assets/images/papers/SLAM++/ICP_breif_description.png]]

- *Idea*: iterate to find alignmen
- *Wanted*: translation t and rotation R that minimizes the sum of the
  squared error:

[[/Assets/images/papers/SLAM++/ICP_Minimum_Error.png]] Where $x_i$ and
$p_i$ are corresponding points

--------------

** ICP Refinement
   :PROPERTIES:
   :CUSTOM_ID: icp-refinement
   :END:

Noisy raw detected poses refined with ICP against object model: - False
detections removed with threshold after ICP - Accurate object-camera
constraints achieved and added to graph

--------------

** Camera Tracking
   :PROPERTIES:
   :CUSTOM_ID: camera-tracking
   :END:

Camera is tracked using ICP against a depth map raycasted from the
current whole world model at last pose: - Very much the same as
KinectFusion; but we track against our perfect, *complete object
models* - Full prediction and *occlusion handling*, even for recently
acquired objects.

--------------

** Graph Optimisation
   :PROPERTIES:
   :CUSTOM_ID: graph-optimisation
   :END:

- Reprsent the world as a graph
- Each node stores estimated $SE(3)$ pose (rotation and translation
  relative to a fixed world frame)

*Goal*: minimize the sum over all measurement constraints

--------------

** Graph Optimisation Presentation
   :PROPERTIES:
   :CUSTOM_ID: graph-optimisation-presentation
   :END:

- $T_{woj}$: $SE(3)$ pose of object $j$
- $T_{wi}$: SE(3) pose of the historical pose of the camera at timestep
  $i$.
- $Z_{i,oj}$: constraints, which links one camera pose and one object
  pose

--------------

** Map Representation
   :PROPERTIES:
   :CUSTOM_ID: map-representation
   :END:

*A Pure Graph of Objects*:

A pose graph where all constraints are 6DoF

--------------

** Moved Object Detection
   :PROPERTIES:
   :CUSTOM_ID: moved-object-detection
   :END:

This paper demonstrate the ability to detect the movement of objects,
which fail ICP gating due to inconsistency

--------------

** Relocalisation
   :PROPERTIES:
   :CUSTOM_ID: relocalisation
   :END:

 When camera tracking is lost the system enters a relocalisation mode.

The matched vertex with highest vote in the long-term graph is used
instead of the currently observed vertex in the local graph and camera
tracking is resumed from it, discarding the local map.

--------------

** Augmented Reality with Objects
   :PROPERTIES:
   :CUSTOM_ID: augmented-reality-with-objects
   :END:

#+CAPTION: center 80%
[[/Assets/images/papers/SLAM++/AugmentedRealityWithObjects.png]]

The ability to semantically predict complete surface geometry from
partial views allows novel context aware AR capabilities such as path
finding, to gracefully avoid obstacles while reaching target objects

--------------

* Any Questions?
  :PROPERTIES:
  :CUSTOM_ID: any-questions
  :END:

--------------

* Conclusions
  :PROPERTIES:
  :CUSTOM_ID: conclusions
  :END:

- Using *high performance 3D object recognition* in the loop permits a
  new approach to real-time SLAM with large advantages in terms of
  *efficient and semantic scene description*.
- Suit to locations like *the interiors of public buildings* with many
  *repeated, identical elements*.
